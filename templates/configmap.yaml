apiVersion: v1
kind: ConfigMap
metadata: 
    name: catalogue
    namespace: roboshop-dev
    labels:
        project: roboshop
        tier: backend
        service: catalogue
data:
    MONGO: "true"
    MONGO_URL: {{ .Values.configmap.mongoURL }}
